fn main()
{
    let a be double;
	let b be double;
	read(a);
	read(b);
	write((a + a) * a);
	write(a * a / b);
    //if (a less b) {
    //    write(a);
    //}
    //else {
    //    write(b);
    //}
}